import Layout from '../components/Layouts/DefaultLayout';

const About = () => (
	<Layout public={true}>
		<p>A starter kit for universal (if you want) React apps with GraphQL.</p>
		<p><a href="https://github.com/gabestein/graphql-starter">GitHub</a></p>
	</Layout>
);

export default About;
